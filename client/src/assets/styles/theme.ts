import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {},
  palette: {
    primary: {
      main: '#1a1a24',
    },
    secondary: {
      main: '#353549',
    },
    info: {
      main: '#FFFFFF',
    },
  },
});

export default theme;
